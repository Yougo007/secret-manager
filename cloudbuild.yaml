steps:
- name: 'gcr.io/cloud-builders/docker'
  entrypoint: 'bash'
  args: ['-c', 'docker login --username=$$USERNAME --password=$$PASSWORD']
  secretEnv: ['USERNAME', 'PASSWORD']
availableSecrets:
  secretManager:
  - versionName: yougo-projects-labs/yougo-projects-labs/secrets/USERNAME/versions/1
    env: 'PASSWORD'
  - versionName: yougo-projects-labs/yougo-projects-labs/secrets/USERNAME/versions/1
    env: 'USERNAME'
